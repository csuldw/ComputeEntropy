<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zola.study.dao.IUserMapper">

    <select id="findAge" parameterType="int" resultType="String">
        select age from tb_user WHERE id=#{id} 
    </select>
</mapper>